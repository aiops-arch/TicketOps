services:
  - type: web
    name: ticketops
    runtime: node
    plan: free
    buildCommand: npm install && npm run build:web
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: TICKETOPS_API_BASE
        sync: false
